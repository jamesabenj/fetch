<% if @dog.errors.any? %>
<div id="error_explanation">
  <ul>
  <% @dog.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>

<%= form_for :dog do |f|%>
  <%=render partial: 'form', locals: {f: f}%>
  <%=f.submit "Submit"%>
<%end%>